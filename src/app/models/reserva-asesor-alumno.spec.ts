import { ReservaAsesorAlumno } from './reserva-asesor-alumno';

describe('ReservaAsesorAlumno', () => {
  it('should create an instance', () => {
    expect(new ReservaAsesorAlumno()).toBeTruthy();
  });
});
